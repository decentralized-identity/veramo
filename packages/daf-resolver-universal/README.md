# DAF DID Document resolver

```typescript
const didResolver = new DafUniversalResolver({ url: 'https://uniresolver.io/1.0/identifiers/' })
const didDoc = await didResolver.resolve('did:web:uport.me')
```
